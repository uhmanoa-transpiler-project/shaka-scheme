#ifndef BIGINT_H
#define BIGINT_H
#include <cmath>
#include <boost/variant.hpp>
#include <typeinfo>
#include <vector>
#include <string>
//#include <stdlib>
/*
class BigInt
{
    ~BigInt() {}
    
    // Description of Constructors below:
    //  store value in vector<int> given a long long int
    //  store value in vector<int> given a string
    
    BigInt(long long int llival) { llint_to_array(llival); }
    BigInt(string sval) { string_to_intarray(sval); }
    
public:
    
    // Operations to be overloaded:
    //      Add
    //      Subtract
    //      Multiply
    //      Exponents
    //      Factorial
    //      Divide
    //      Modulo
    
private:
    
    // Vector of ints
    vector<int> value;
    int length;
    
    // Functions for constructors
    void int_to_array(long long int v);
    void string_to_intarray(string s);
};


// Private Functions for constructors

void BigInt::llint_to_array(long long int v)
{
}

void BigInt::string_to_intarray(string s)
{
}
*/


#endif // BIGINT_H
